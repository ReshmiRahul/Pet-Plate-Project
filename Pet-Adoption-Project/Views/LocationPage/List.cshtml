@model IEnumerable<PetAdoption.Models.LocationDto>

@{
    ViewData["Title"] = "Locations List";
}

<h1>Locations List</h1>

<p>
    <a asp-action="New" class="btn btn-primary">Create New Location</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.City)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.LocationId" class="btn btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.LocationId" class="btn btn-info">Details</a>
                    <form asp-action="ConfirmDelete" asp-route-id="@item.LocationId" method="get" style="display:inline-block;">
                        <input type="submit" value="Delete" class="btn btn-danger" />
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
